---
wave: 4
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - backend/routes/products.js
  - backend/routes/customers.js
  - backend/routes/orders.js
autonomous: true
requirements_addressed: []
---

<objective>
Refactor all backend route handlers to use proper PostgreSQL parameterized queries (`$1`, `$2`) instead of dynamic string concatenations for WHERE clauses, preventing SQL injection.
</objective>

<must_haves>
- [ ] No string concatenations used for SQL WHERE clauses
- [ ] `pg` parameterized queries used across all route files
</must_haves>

<tasks>
<task>
<description>Update dynamic queries to use parameterization</description>
<read_first>
- backend/routes/products.js
- backend/routes/customers.js
- backend/routes/orders.js
</read_first>
<action>
Review `backend/routes/products.js`, `backend/routes/customers.js`, and `backend/routes/orders.js`.
Find any dynamic SQL construction like `WHERE id = ` + id or string interpolations inside SQL queries.
Replace them with parameterized queries: `query('SELECT * FROM table WHERE id = $1', [id])`.
Ensure all `db.query()` calls correctly format parameters as arrays.
</action>
<acceptance_criteria>
- `backend/routes/products.js` does not contain `WHERE id = \$\{` or `WHERE id = "`
- `backend/routes/orders.js` uses `$1` for parameters in SQL queries
- `backend/routes/customers.js` uses `$1` for parameters in SQL queries
</acceptance_criteria>
</task>
</tasks>
