---
wave: 4
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - backend/routes/payments.js
autonomous: true
requirements_addressed: []
---

<objective>
Wrap payment creation and inventory deduction in a single PostgreSQL database transaction to ensure atomicity.
</objective>

<must_haves>
- [ ] `payments.js` updated to use `db.transaction()` wrapper from db.js
- [ ] Order creation and stock deduction occur within the same transaction context
</must_haves>

<tasks>
<task>
<description>Wrap payment and inventory logic in a transaction</description>
<read_first>
- backend/routes/payments.js
- backend/db.js
</read_first>
<action>
Update the webhook handler in `backend/routes/payments.js` (or wherever payment success is verified and inventory is deducted).
Ensure the logic that inserts the order into the `orders` table, inserts `order_items`, and updates `product_variants.stock` is wrapped in the `db.transaction(async (client) => { ... })` wrapper.
Make sure to pass the transaction `client` to all database queries within that block instead of using the global pool directly.
</action>
<acceptance_criteria>
- `backend/routes/payments.js` contains `db.transaction(async`
- `backend/routes/payments.js` updates `product_variants` inside the transaction
</acceptance_criteria>
</task>
</tasks>
