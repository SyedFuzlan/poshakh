---
wave: 3
depends_on: [08-02]
files_modified:
  - backend/migrations/20260510000001-indexes.js
autonomous: true
requirements_addressed: []
---

<objective>
Add database indexes on frequently queried columns: customers(phone, email), orders(status), products(category_id).
</objective>

<must_haves>
- [ ] A new db-migrate migration file for creating indexes
- [ ] Indexes created on the correct columns
</must_haves>

<tasks>
<task>
<description>Create migration for indexes</description>
<read_first>
- backend/migrations/20260510000000-initial.js
</read_first>
<action>
Create a new migration file: `backend/migrations/20260510000001-indexes.js`.
In the `up` function, use `db.runSql()` to execute:
- `CREATE INDEX idx_customers_phone ON customers(phone);`
- `CREATE INDEX idx_customers_email ON customers(email);`
- `CREATE INDEX idx_orders_status ON orders(status);`
- `CREATE INDEX idx_products_category ON products(category_id);`
In the `down` function, `DROP INDEX` for each of these indexes.
</action>
<acceptance_criteria>
- `backend/migrations/20260510000001-indexes.js` contains `CREATE INDEX idx_customers_phone`
- `backend/migrations/20260510000001-indexes.js` contains `CREATE INDEX idx_orders_status`
</acceptance_criteria>
</task>
</tasks>
