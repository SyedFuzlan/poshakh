---
wave: 2
depends_on: [08-01]
files_modified:
  - backend/package.json
  - backend/migrations/20260510000000-initial.js
autonomous: true
requirements_addressed: []
---

<objective>
Migrate all CREATE TABLE statements from the old SQLite initialization in db.js to proper db-migrate migration files.
</objective>

<must_haves>
- [ ] db-migrate installed
- [ ] Initial migration created for all tables
</must_haves>

<tasks>
<task>
<description>Install db-migrate</description>
<read_first>
- backend/package.json
</read_first>
<action>
Add `db-migrate` and `db-migrate-pg` to `backend/package.json` dependencies. Update package-lock.
Add a `"migrate"` script to package.json: `"migrate": "db-migrate up"`.
</action>
<acceptance_criteria>
- `backend/package.json` contains `"db-migrate":`
- `backend/package.json` contains `"db-migrate-pg":`
- `backend/package.json` contains `"migrate": "db-migrate up"`
</acceptance_criteria>
</task>

<task>
<description>Create initial migration</description>
<read_first>
- backend/db.js
</read_first>
<action>
Create the first migration file: `backend/migrations/20260510000000-initial.js` (or similar timestamp).
Translate the SQLite CREATE TABLE statements found in the original `db.js` (products, categories, product_images, checkouts, product_variants, customers, orders, order_items, order_status_history, inventory_logs, audit_logs, promo_codes, site_settings, refresh_tokens, email_verification_tokens, password_reset_tokens) into standard PostgreSQL `CREATE TABLE` syntax inside the `up` function of the migration, using `db.runSql()`.
Ensure PostgreSQL specifics are handled (e.g., `SERIAL PRIMARY KEY` instead of `INTEGER PRIMARY KEY AUTOINCREMENT`, timestamps appropriately set).
In the `down` function, `DROP TABLE` for all these tables.
</action>
<acceptance_criteria>
- `backend/migrations/20260510000000-initial.js` contains `exports.up`
- `backend/migrations/20260510000000-initial.js` contains `SERIAL PRIMARY KEY`
- `backend/migrations/20260510000000-initial.js` contains `CREATE TABLE products`
</acceptance_criteria>
</task>
</tasks>
