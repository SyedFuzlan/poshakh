# Testing

## Current State
- **Automated Tests**: Minimal. Backend and Frontend have `test` scripts in `package.json` that are currently placeholders.
- **Manual Testing**: Primary method via the Admin Dashboard (`/dashboard/index.html`) and frontend storefront preview.

## Verification Paths
- **Checkout Flow**: Tested manually against Razorpay test mode.
- **Admin Actions**: Verified via dashboard (product CRUD, inventory updates).
